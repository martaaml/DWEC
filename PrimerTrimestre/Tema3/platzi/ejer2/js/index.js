/**
 * Transforma un Array de objetos a un Array de strings
 */

export function getNames(array) {
    const getNames = (name, lastname, age) => alert(alerts[(Math.floor(Math.random() * alerts.length))] + `, ${name},${lastname},${age}!`)
  
  }
  