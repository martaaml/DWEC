/**
 * 
 * @multiplyElements
 * Tienes un array de números, tu reto es retornar los números de ese array multiplicados por dos.
 * 
 */
function multiplyElements(array){
    array[1,7,3,89,43];
let array=(num1,num2)=>num1*num2;
}


export function multiplyElements(array) {

    return array.map(n=> n* 2);
 }


 export function multiplyElements(array) {
    input[1, 7, 3, 89, 43];
    output[4, 8, 7, 9, 45, 14];
    array = (input, output) => input * output;
    return multiplyElements;
 }