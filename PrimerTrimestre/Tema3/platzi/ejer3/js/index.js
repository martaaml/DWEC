export function addNewAttr(array) {
    const dates = ("name", "price", "stock");
    for (I = 0; i.array.lenght; i++) {
      let taxes = price.math.floor() * 0.19;
    }
    return addNewAttr;
  }
  